[project]
name = "examplepydantic"
version = "0.1.0"
description = "Exemplo criação modelo com validação tipo Dados, serializar e desserializar objetos para garantir a integridade dados."
authors = [
    {name = "Marcelo Vasselai",email = "mvasselai@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
